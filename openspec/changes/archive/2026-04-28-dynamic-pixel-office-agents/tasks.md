## 1. SDD And Assets

- [x] 1.1 Create proposal, design, specs, and tasks for the dynamic pixel office change.
- [x] 1.2 Copy bundled office support assets from the `the-dev-squad` reference when needed.

## 2. Simulator Office

- [x] 2.1 Extend simulator step metadata with office phase, active agents, and interaction copy.
- [x] 2.2 Add a reusable dynamic pixel office component with all five agents visible.
- [x] 2.3 Wire the Flow Simulator to render the dynamic office instead of the single-agent stage.
- [x] 2.4 Add responsive, pixel-art office styles with reduced-motion support.

## 3. Verification

- [x] 3.1 Update frontend tests for all-agent visibility, interaction phases, reduced motion, and existing 5 second pacing.
- [x] 3.2 Run `openspec validate dynamic-pixel-office-agents --strict`.
- [x] 3.3 Run `npm run --prefix frontend test -- --run`.
- [x] 3.4 Run `npm run --prefix frontend build`.
