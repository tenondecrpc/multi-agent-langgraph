apiVersion: v1
kind: Service
metadata:
  name: dev-squad-backend
spec:
  selector:
    app: dev-squad-backend
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
